# Claim ledger — new geometry future observables

STATUS: CONTRACT_FROZEN_NO_EVALUATION_RUN
DATE: 2026-07-19

## Allowed claims before evaluation

- A new question has been defined under `SQUARE_BOX_2P4`.
- The prior R-VAR closure remains intact:

```text
CLOSED_NEGATIVE_RESULT [GEOMETRY_SPECIFIC]
```

- The contract freezes a new patch geometry, new seed ranges, order-only observables, support
  gates, Minkowski non-degeneration gate, terminal precedence, and evidence directory.
- No evaluation data has been generated by this evidence package.

## Forbidden claims

- This is not a repair of R-VAR.
- This is not a re-evaluation of R-VAR.
- This does not supersede `CLOSED_NEGATIVE_RESULT [GEOMETRY_SPECIFIC]`.
- This does not claim horizon reconstruction.
- This does not claim that the new patch removes Minkowski degeneracy until the frozen evaluation
  is actually run.
- A future contract/design failure may not be reported as a scientific negative result.

## Pending claims

These require a later explicit evaluation run under the frozen contract:

- Whether the `SQUARE_BOX_2P4` Minkowski control is non-degenerate.
- Whether BH-vs-MINK future-observable dispersion differs at the primary endpoint.
- Which typed terminal is emitted by the evaluation.

## Sealed result — 2026-07-19

Terminal:

```text
BH_MINK_DISPERSION_DIFFERENCE_DETECTED
layer=scientific
geometry=SQUARE_BOX_2P4
previous_RVAR_closure=INTACT
```

Primary endpoint:

```text
PRIMARY_INTENSITY_NEW = 9600.0
valid_BH_MINK_pairs = 24/24
```

Frozen scientific contrasts:

```text
D_L(seed) = cv_L_BH(seed) - cv_L_MINK(seed)
D_V(seed) = cv_V_BH(seed) - cv_V_MINK(seed)
```

Observed primary values:

```text
median(D_L) = 0.4987114017481817
median(D_V) = 0.47639013575705436
p_perm(D_L) = 1.1920928955078125e-07
p_perm(D_V) = 1.1920928955078125e-07
```

Minkowski control:

```text
MINK_CONTROL_NONDEGENERATE_ON_NEW_GEOMETRY
MINK median cv_L = 0.019746455489398754
MINK median cv_V = 0.11488594284354918
non_degeneracy_condition = median(cv_V_MINK) >= 0.05
```

Boundary:

```text
Previous R-VAR closure CLOSED_NEGATIVE_RESULT [GEOMETRY_SPECIFIC] remains intact and untouched.
This is a new scientific question under SQUARE_BOX_2P4.
```

Allowed interpretation:

- The predefined future-observable dispersion summaries distinguish BH from MINK in the new
  square-box geometry under the frozen contract.

Forbidden interpretation:

- This does not localize a horizon.
- This does not reconstruct geometry.
- This does not repair, reopen, supersede, or reinterpret the prior R-VAR closure.
