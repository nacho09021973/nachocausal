# Comité Decision 006 — pr003-relational-horizon-candidate-adjudication

> Produced by `/comite`. The committee PROPOSES; the user AUTHORISES. The committee never executes
> a one-way or outward-facing action (the blind validation run, a commit/push, anything
> irreversible), never tunes a frozen threshold post-hoc, and never makes a reconstruction claim.
> Guardrails: `NO_RECONSTRUCTION_CLAIM`, `NO_POST_HOC_TUNING`, `NO_THRESHOLD_LOOSENING`,
> `NO_GROUND_TRUTH_LEAKAGE`, `RESPECT_SEAL_FREEZE`.

## 1. Decision question
Which of C1 and C2 (defined in `dev/X0_Qn_wellposedness_NOTES.md` §11) is a genuinely covariant,
purely order-theoretic, FALSIFIABLE candidate for a relational quantum precursor of the horizon,
able to distinguish trapping from singularity AND truncation, WITHOUT introducing a covert notion
of "exterior", embedding, or classical localisation? The committee's product is a CLOSED,
FALSIFIABLE MATHEMATICAL DEFINITION of the candidate observable — nothing is sealed, run, or
implemented. Adjudicate C1, C2 separately on 8 axes: (1) intrinsicality, (2) orientation [does C1
need an order-undefined "exterior"?], (3) normalisation across antichains of different size/depth,
(4) Hayward [persist for a regular BH with NO singularity], (5) flat control, (6) truncation,
(7) quantum covariance, (8) semiclassical correspondence [near-horizon concentration is a downstream
PREDICTION]. Critical objection to resolve before promoting C1: "outward crossing" may smuggle the
forbidden spatial direction. Constraint: do NOT fuse C1+C2 yet. Close with one of
`PROMOTE_C1_AS_PRIMARY_OBSERVABLE` | `PROMOTE_C2_AS_PRIMARY_OBSERVABLE` |
`PROMOTE_C1_WITH_C2_AS_INDEPENDENT_VALIDATOR` | `REFINE_CANDIDATES_BEFORE_PROMOTION` |
`REJECT_BOTH_AS_NOT_INTRINSIC`. User's prior (non-binding) vote, subject to the "outward" audit:
`PROMOTE_C1_WITH_C2_AS_INDEPENDENT_VALIDATOR`.

## 2. Verified state
Facts checked **this session**, each with its command / file:line.
- **Seal:** `make verify-seal` → `thresholds.py sha256: 6e2c38881234cef48e859096b46f261cfa83ea8a2f6c955cc1dbc42537bfefd4`. Matches `docs/preregistration_002.md:8` and `docs/preregistration_003.md:9`. Sealed path INTACT.
- **Git:** branch `main`, HEAD `aef48ce` ("docs: PR-003 Fase #3 — comité decision 005"). Working tree: only four untracked dev notes (`dev/X0_Qn_wellposedness_NOTES.md`, `dev/COMITE_seat_asymptotic_statistics.md`, `dev/INTRINSIC_OBSERVABLE_AUDIT_NOTES.md`, `dev/MEMO_random_orders_identifiability.md`); `nachocausal/` unmodified (0 M).
- **Validation results:** `results/validation.json` is the PRIOR prereg-002 PASS (untouched); `results/formula_*.json` are a sibling `formula`-branch agent's artefacts, irrelevant and not on main. **No validation run is involved in this question.**
- **Scope:** reversible dev-phase adjudication of candidate DEFINITIONS; the candidates' full text is `dev/X0_Qn_wellposedness_NOTES.md` §11 (with §9 = `COMMON_CHANNEL_BLOCKED_BY_SUPPORT_OR_DOMAIN` audit, §10 = the reframing of the quantum object as a relational predicate `𝔥(C)` with localisation EMERGENT only semiclassically). `/auditor` precondition does not apply (this is not a PROCEED built on already-claimed results).

## 3. Dossier
Files and references the chair supplied to the committee:
- `dev/X0_Qn_wellposedness_NOTES.md` §9 (common-channel audit), §10 (reframing), §11 (candidates C1/C2/C3).
- `dev/INTRINSIC_OBSERVABLE_AUDIT_NOTES.md`, `dev/MEMO_random_orders_identifiability.md`, `docs/comite/comite_decision_005_*.md`.
- `docs/preregistration.md`, `docs/preregistration_001_addendum.md`, `docs/preregistration_002.md` (+ result), `docs/preregistration_003.md`, `docs/estimator_v2_seal.md`/`_freeze.md`, `docs/pr003_leakage_gate.md`.
- `nachocausal/generator.py` (geometry, tortoise `func`, EF in/out), `nachocausal/estimator.py` (order-only observable, `verify_order_only`), `nachocausal/thresholds.py` (seal, `r_S`, box, seeds), `tests/test_leak.py`.
- `biblioteca/derived-md/` Eichhorn–Gamito–Stokes *"Towards black-hole horizons and geodesic focusing in causal sets"* (arXiv:2605.06813); Surya LRR *"The causal set approach to quantum gravity"*; Benincasa–Dowker 2010 (`1001.2725`); Bombelli 1987 PhD.
- Candidate summary — **C1** = causal bottleneck / minimal outward-flux antichain: `β(A)=#{covering-links from order-ideal down(A) to its complement}/|A|`, predicate "∃ antichain `A` with `β` a pronounced local minimum", deficit `Δ=β̄_bulk−β`, `β̄_bulk` self-calibrated. **C2** = future-overlap collapse: `κ(A)=|⋂_i J⁺(a_i)|/E_indep`, reference from Benincasa–Dowker interval abundances, predicate "∃ wavefront `A` with `κ≫1`". **C3** = future width-collapse — already REJECTED (fails Hayward). Forbidden covert rewrites: `relphi`, EGS expansion Θ, outgoing spatial direction, null coordinates, distance-to-horizon. Standing caveat: order-only + Hayward-passing is NECESSARY not SUFFICIENT for intrinsicality.
- Binding guardrails: `RESPECT_SEAL_FREEZE` (`6e2c3888…`), `NO_GROUND_TRUTH_LEAKAGE` (observable σ-algebra order-only), `NO_RECONSTRUCTION_CLAIM` (at most apparent/trapped, finite 1+1D), `NO_POST_HOC_TUNING`.

## 4. Expert briefs (wave 1 — blind, parallel)
### Reproducibility engineer brief
- **Proposed artefact(s):**
  - Measurement script: `dev/measure_membrane_c1c2.py` (or split `dev/measure_bottleneck_c1.py` + `dev/measure_overlap_c2.py`), following the committed `dev/measure_*.py` convention with the mandatory docstring header that declares "dev/, DEV_SEEDS only, never imports/evaluates VALIDATION_SEEDS, does NOT modify the sealed estimator or any threshold" exactly as `dev/explore_observable.py:1-26` and `dev/measure_pr003.py:1-25` already do. β(A)/κ(A) consume **only** the boolean past matrix `C` and quantities derived from it (links/covering, antichains, `J⁺`/`down`), and coordinates enter **only** to score `d_perp` after the order-only statistic is fixed (the `dev/measure_pr003.py` discipline, `dev/measure_pr003.py:23-25`).
  - A `dev/*_NOTES.md` companion recording the spec exactly as the existing `dev/X0_Qn_wellposedness_NOTES.md` §11 does. No new file under `nachocausal/`, `docs/preregistration_*`, or `results/`: a future C1/C2 probe is a **dev exploration**, NOT a prereg freeze and NOT a sealed-path run.
  - It must reuse the sealed primitives read-only: `nachocausal/generator.py:88 past_matrix_fast`, `:37 numpy_sprinkle`, `:53 assert_coordinate_uniform`, and `nachocausal/estimator.py` for the abstract poset; β/κ are new **dev** functions, not additions to the sealed estimator.
- **Environment & seal:**
  - Two distinct environments, do not conflate. Sealed/validation env is pure numpy pinned `1.26.4` (`nachocausal/thresholds.py:18 PINNED_NUMPY`, enforced by `assert_environment` at `thresholds.py:21`; `Makefile:2`). The external Minz dev env (`~/cs-horizon-reuse-check/venv_minz`, numpy<2, `CLAUDE.md:28-29`) is only for the `prototype_o.py` cross-check and is irrelevant to a numpy-only β/κ probe.
  - Re-verify seal **before and after** any dev probe: `make verify-seal` must print `thresholds.py sha256: 6e2c38881234cef48e859096b46f261cfa83ea8a2f6c955cc1dbc42537bfefd4` — confirmed live this session (matches `docs/preregistration_002.md:8` and `docs/preregistration_003.md:9`). Note `docs/estimator_v2_seal.md:8` still records the **older instrument seal** `2f4c4a99…`; superseded by the prereg-002/003 re-seal `6e2c3888…`. A future probe re-verifies against `6e2c3888…`.
  - Package-diff-clean: capture `pip freeze` and assert numpy==1.26.4; the dev probe must not `pip install` anything.
- **Provenance capture (the run must record):** HEAD commit (`aef48ce` now) + `git status --porcelain` (must show only the four untracked dev memos); `pip freeze`; `uname -a` / platform; numpy version; the seed band used (**`DEV_SEEDS` = `thresholds.py:57`**, or the 40-seed `EXPLORE_POOL` used by `dev/measure_pr003.py`) with an explicit assertion it is disjoint from `VALIDATION_SEEDS` (`thresholds.py:72`); the RNG seed for the β̄_bulk / E_indep null draws; start/end UTC timestamps; the seal SHA asserted pre+post (the pattern in `docs/preregistration_003.md:106,166`).
- **Run mechanics:**
  - Single foreground invocation in the sealed venv (`.venv/bin/python dev/measure_membrane_c1c2.py`). If the antichain/overlap search is heavy (κ's `⋂J⁺` and the bulk-antichain null can be combinatorial), run as a background job logging to `dev/membrane_c1c2.log`.
  - A guard aborts cleanly by raising **before** any score: assert the seal SHA at entry, assert numpy==1.26.4 via `assert_environment`, assert seed-band disjointness; any failure raises and writes nothing into `results/`.
  - **Reversible pre-flight only.** No `validate.run()`, no fresh-seed draw, no re-seal, no `results/formula_*.json`. The committing-step machinery (`make gate`, single blind `validate.run` on `VALIDATION_SEEDS`) is explicitly NOT invoked (`docs/estimator_v2_seal.md:54-59`).
- **Reproducibility risks / ambiguities:**
  - **β(A) partition provenance (C1, the load-bearing leakage risk):** "outward / `y∉down(A)∪A`" must be computed **purely** from the order-induced partition `down(A)` vs complement (`dev/X0_…NOTES.md:646-650`), never from an embedding side-label. A future probe must extend `tests/test_leak.py`'s `test_estimator_source_is_coordinate_free` to the β-module (leakage gate contract 1, `docs/pr003_leakage_gate.md`) and run Guard-v's relabel-conjugation (`nachocausal/estimator.py:164 verify_order_only`) on β/κ. **Risk:** if "outward" is silently keyed to coordinate `[:,1]` it reduces to the forbidden outgoing-spatial direction; the coordinate-free source test makes that fail mechanically.
  - **Null-statistic seed determinism:** β̄_bulk and E_indep are MC draws over the causet's own structure; their RNG seed must be frozen and logged (cf. `GATE_NULL_MC_SEED=20260621`, `thresholds.py:139`) or κ/Δ are not bit-reproducible.
  - **Truncation-control symmetry (both, fatal for C2):** falsifiers require the MINK twin sprinkled with the *identical* truncation/box and *same cloud per seed* (`thresholds.py:51` "BH and MINK differ ONLY in causality"). If the probe does not reuse the same cloud and patch boundary for both arms, a κ_BH>1 signal is unattributable (box-merge vs focusing).
  - **[UNVERIFIED]** Search non-determinism: "find the antichain whose β is a local minimum" / "∃ wavefront with κ≫1" is a combinatorial search whose tie-breaking can be label-dependent (the wound of `dev/measure_pr003.py:1-12`). A future spec must pin an order-canonical search or report a lower bound; §11 gives only the predicate, no closed search procedure.
  - **Antichain-size normalisation (axis 3):** β normalised by `|A|`, κ by E_indep; the probe must log the `|A|` and causal-depth distribution actually sampled, else cross-antichain comparability of Δ/κ is not reproducible from the artefact alone.

### Mathematician brief
- **Computability.** Decidable on the order relation alone (no embedding): for a *given* finite causet `C`, the cover relation (links: `x≺y` with empty open interval — Benincasa–Dowker_2010:40, "I(x,y) contains only x and y"), `down(A)`/`J⁻`, `J⁺`, intersections `⋂J⁺(aᵢ)`, antichain membership, and Dilworth width are all order-invariant and computable in poly time on the relation matrix. **C1's partition `down(A)` vs complement is genuinely order-defined** (`down(A)` = the order ideal generated by `A`); the *separator/cut* notion is the standard inextendible antichain that "separates C into Fut/Past" (The-causal-set-approach…:882–885: `C = Fut(A) ⊔ Past(A) ⊔ A`). **But "minimal-outward-flux antichain" is NOT yet a closed object**: (i) the predicate quantifies "∃ antichain A with β(A) a pronounced local minimum" over an exponentially large antichain lattice with no specified search class — "pronounced local minimum" and "local" are undefined order-theoretically; (ii) the EGS "missing-link" pathology (same source :886, a Past-element linked to a Fut-element "bypassing A") means an inextendible antichain is *not* a clean cut, so β counted on covers can be bypassed — the cut is leaky and the count is sensitive to which antichain in the family is chosen. **C2's `S(A)=⋂ J⁺(aᵢ)` is fully closed and computable**; the non-closed part is `E_indep[|S(A)|]` — "anomalous" is only well-defined once that normaliser is a pre-specified order functional, which it currently is not (see below).
- **Order observable.**
  - **C1**: `β(A) = #{cover-pairs (x,y): x∈down(A), y∉down(A)∪A}/|A|`, `Δ(A)=β̄_bulk−β(A)`. Every *symbol* is order-derived — `down(A)` is the generated order ideal, covers are intrinsic, `β̄_bulk` is self-calibrated from the causet's own cover statistics. **The forbidden "exterior" is NOT smuggled by the partition** (ideal vs complement is order-canonical, the past/future direction is intrinsic — confirmed by Surya's antichain-separation). **The smuggle risk is in the word "outward".** As literally defined, `β` counts covers from `down(A)` to its complement — *future-directed* links escaping the ideal — which is order-defined and fine. The danger is only if "outward" is later read as the *spatial* (outgoing-null) direction; the definition as written does NOT do this, but the predicate's *motivation* ("one-way membrane, outward influence suppressed", §11.1.3) presumes an in/out asymmetry that the symmetric ideal-boundary count does not by itself supply. **Flag: C1 must drop "outward" and commit to "cover-links escaping the order ideal `down(A)`", or it risks an unstated orientation.** A future ideal and its complementary filter give a *temporal* asymmetry, not a spatial one — acceptable; β counts only the down→up crossing, which is the temporally-forward boundary flux and is order-legitimate.
  - **C2**: `κ(A)=|⋂ᵢ J⁺(aᵢ)| / E_indep[|⋂ᵢ J⁺(aᵢ)|]`. Numerator order-pure. **Denominator is the open symbol.** "Expected if futures met generically" has no parameter-free order definition. Benincasa–Dowker `C_k`/interval-abundance gives `⟨Nᵐ_d⟩` that depends explicitly on **dimension d and density ρV** (The-causal-set-approach…:1900–1909, eq. 51 `∝ e^{−ρV}`, and "depends on dimension"; Benincasa_Dowker_2010:123 `Nᵢ` = number of (i+1)-element inclusive intervals). So the BD baseline is a *dimension- and density-keyed* expectation, NOT a parameter-free intrinsic null for an m-fold future-intersection — and it is an *interval/order-interval* abundance, not a *common-future of an m-antichain* abundance. **No smuggled exterior in C2**, but the normaliser as cited is under-specified and not obviously the right combinatorial object.
- **Relevant invariants.**
  - *Antichain / inextendible antichain as cut*: Surya, The-causal-set-approach…:880–885 (defn; `C=Fut⊔Past⊔A`), with the missing-link caveat :886. This is C1's backbone — and its weakness (leaky cut).
  - *Order ideal / filter*: `down(A)` = generated ideal; complement = filter. Standard order theory; Surya's Fut/Past split is the causet instance.
  - *Dilworth width*: max antichain size — used (and rejected) in C3; relevant as the order-theoretic "transverse extent". C3 correctly retired (§11.3) on Hayward.
  - *Future-volume `V(x)=|J⁺(x)|`*: the one-point functional already in use (X0 §1; estimator). C2's `|⋂J⁺|` is the m-point joint generalisation — the σ(C_loc)∖σ(V) "focusing" channel X0 §2 isolates.
  - *Interval abundances `Nᵐ_d` / `C_k`*: Benincasa_Dowker_2010:123; Glaser–Surya via Surya :1900–1920. The *only* biblioteca-grounded intrinsic null, and they are dimension/density-dependent self-averaging diagnostics — a *manifoldness/curvature* test, not a generic-overlap expectation.
- **Analytic / continuum target.**
  - **C1 → one-way membrane / marginally-trapped surface.** A minimum of forward cover-flux across an order ideal boundary is the discrete analogue of a null hypersurface across which outgoing influence is suppressed — EGS's apparent horizon as "marginally outer trapped surface, Θ_out=0 at r=2M" (Towards-black-hole…:201,223–225). C1 targets this *without* EGS's ladders/expansion.
  - **C2 → geodesic focusing.** `κ≫1` (over-reconvergent common future of a spread wavefront) is the order analogue of `Θ<0` cross-sectional contraction — EGS eq. 11 `Θ=(1/A)dA/dλ`, "when there is geodesic focusing we expect this distance to decrease" (Towards-black-hole…:217,230,282). C2 claims distinctness from EGS Θ by using *set-overlap only* (no predistance/ladder/radius/in-out), which is order-theoretically genuine — but the continuum correspondence (does `|⋂J⁺|` track `∫Θ`?) is unestablished and is properly a *downstream prediction*, not a definitional input.
- **Caveats.**
  - C1's existential predicate ("∃ antichain with pronounced local minimum") is **not a closed definition** until the antichain search class and "local minimum / pronounced" are given as order functionals. As stated it is an underspecified optimisation, not an invariant. [§11.1.1 wording]
  - The inextendible-antichain "cut" is leaky (missing-link bypass), so C1's β is sensitive to *which* separating antichain is chosen — robustness of `Δ(A)` across the antichain family is unproven. [The-causal-set-approach…:886]
  - "Outward" in C1 must be reduced to "forward cover-flux escaping the order ideal `down(A)`" to be provably free of a spatial direction; the membrane *motivation* presumes an in/out split the symmetric ideal-boundary count does not supply. [§11.1.1 vs §11.1.3]
  - C2's `E_indep[|S(A)|]` is **not** a parameter-free intrinsic null as cited: the BD/Glaser–Surya abundance is dimension- and density-dependent and is an *interval* count, not the abundance of an m-antichain common future. A genuinely intrinsic normaliser (e.g. conditioning on `|A|=m`, the depths/`|J⁺(aᵢ)|`, and density via a self-averaging baseline) must be pre-specified. [The-causal-set-approach…:1900–1909; Benincasa_Dowker_2010:123]
  - C2's `|⋂J⁺(aᵢ)|` is strongly sensitive to `|A|=m`, causal depth, and the future boundary/truncation (all futures merge at a truncated top end regardless of focusing) — the falsifier (MINK twin, identical truncation, §11.2.7) is *necessary*; the caveat that it is insufficient "if the statistic changes when the temporal cut moves" stands and is unaddressed. [§11.2.7; X0 §2]
  - **Order-theoretic robustness verdict:** C2's *core functional* (`⋂J⁺`) is more sharply closed and computable than C1's (a fixed antichain's `κ` is a clean invariant; C1 requires solving an unspecified min over antichains). But C2's *normalisation* is the weaker link, whereas C1's normalisation (self-calibrated `β̄_bulk`) is order-intrinsic by construction. Net: **C1 is more robust on normalisation and orientation-cleanliness once "outward" is fixed to "ideal-boundary forward flux"; C2 is more robust on definitional closure of the raw statistic but is currently under-specified at the normaliser and most exposed to the |A|/depth/truncation confound.** Neither is yet a *closed* falsifiable definition: C1 lacks a closed quantifier/min; C2 lacks an intrinsic, pre-specified `E_indep`. [§11.1, §11.2, §11.4 — order-only + Hayward-passing is necessary, not sufficient]

### Physicist brief
- **Coordinates & patch:** The sealed step works in **advanced Eddington–Finkelstein** `(t,r)` for the BH branch — the causal rule is built from the tortoise term `func = r + 2 r_S log(|r−r_S|/r_S)` (`nachocausal/generator.py:104`), with the EF in/out asymmetry across `r_S` hard-coded by the `b1/b2/b3` branches (`generator.py:116-125`); MINK uses the flat cone `(dt>0)&(dt≥|Δr|)` (`generator.py:114`). Both share one coordinate-uniform point cloud (constant 1+1D density, EGS md:135; `thresholds.py:51`). The patch is **finite**: `t∈[0,6]` (`T_EDGE=6.0`), `r∈[0.1,1.3]` (`R_CENTER=0.7`,`R_EDGE=1.2`), `r_S=2M=0.5` (`thresholds.py:37-42`) — the box **straddles `r_S`**. Finiteness forfeits the **event horizon outright**: EGS is explicit that the event horizon needs an **infinite sprinkling** (md:173), and that even with an infinite causet one cannot distinguish futureless points at `J⁺` from futureless points near `r=0` (md:175). So C1/C2 can at most target the **apparent/trapped** structure (md:199-201), never the event horizon. **C1 and C2 must remain coordinate-free**: the future/past order is intrinsic to the poset, but neither predicate may import `r`, `r_S`, the tortoise `func`, EF `t_*`, null `(u,v)`, or the EF in/out split — these live only in the hidden generator (NO_GROUND_TRUTH_LEAKAGE).
- **Physical meaning of the signal:**
  - **C1 (bottleneck / outward-flux deficit).** The trapped region is physically a one-way membrane: the apparent horizon is a *marginally outer trapped surface* where both null congruences have non-positive expansion (md:201, 208). A low covering-link bandwidth `β` across a boundary antichain is a defensible order-only image of "outward causal influence suppressed." The **physical hazard** is the word **"outward":** in genuine Schwarzschild the suppressed direction is the *outgoing spatial null* leg — exactly the thing `relphi`/EGS needed an embedding to define (md:142-145 outgoing geodesic `t_*out`). C1 is only admissible if `down(A)` vs complement is a **purely order-theoretic past/complement partition**, never an embedding-inherited interior/exterior. The EF generator itself encodes the asymmetry across `r_S` (`generator.py:119-121`), so a `β`-minimum *will* sit near `r_S` in the manifoldlike regime — but that concentration must be a **downstream prediction**, not built in. **Hayward:** physically sound — a regular BH has an apparent horizon / one-way membrane with **no singularity** (md:157,166-169,195,199), so a true membrane-bandwidth deficit should persist; C1 passing Hayward is the right discriminator (md:195 kills the longest-chain/futureless partition for regular BHs, which is precisely why a membrane predicate is needed). **Box confound:** the `β`-minimum could instead localise on the **`r=0.1` truncation edge** or the box walls — both are low-bandwidth cuts for trivial boundary reasons. The MINK twin with identical truncation is the necessary subtraction.
  - **C2 (future-overlap collapse / focusing).** Geodesic focusing is the genuine trapping signature EGS pursues via expansion `Θ` (md:201-203); anomalous **common future** of an incomparable wavefront is a legitimate order-only proxy. **Hayward:** focusing exists for regular BHs (no singularity needed), so persistence is expected. **The truncation confound is the decisive physical worry and is NOT obviously controllable:** in the finite box *all* futures merge at the upper-`t`/near-`r=0.1` truncated end simply because the box ends. EGS flags exactly this — future-cardinality diagnostics are "affected more strongly by the choice of boundary" and even flat Minkowski in a causal diamond shows future cardinality varying between `n` and `√n` (md:193). So `κ≫1` can be a pure box/singularity artefact. The MINK twin with identical truncation is mandatory but, per the caveat, **insufficient if `κ` moves with the temporal cut**: `E_indep` must be intrinsic and pre-specified, and one must check `κ` is stable under varying the temporal extent, else C2 measures the truncation, not `r=2M`.
- **Sprinkling domain:** Declared region `t∈[0,6]`, `r∈[0.1,1.3]` (`thresholds.py:37-39`), constant-density **Poisson** (coordinate-uniform; constant induced-metric determinant, EGS md:135). Intensity levels `(1500, 3000, 6000, 12000)`, primary `12000`, `ENSEMBLE=20` seeds (`thresholds.py:46-48`). Finite Poisson in a bounded patch forfeits: the asymptotic `J⁺`/event-horizon construction (md:173), any completeness of the trapped region, and any guarantee the boundary antichain (C1) or merged-future locus (C2) is *not* the box wall. Density-stability across the four `N` levels is the only handle that a signal scales as a horizon feature rather than a fixed-boundary artefact.
- **Claim boundary:** Promoting C1 or C2 claims **at most the existence of a relational trapping/focusing transition in a finite 1+1D patch** — an order-invariant predicate `𝔥(C)` (md:585-592). It does **NOT** claim: (i) reconstruction of the metric or of `r_S` (NO_RECONSTRUCTION_CLAIM); (ii) the **event horizon** (needs infinite sprinkling, md:173 — only apparent/trapped is in scope, md:199-201); (iii) **3+1D** anything (1+1D; md:195 notes the 3+1 regular-BH case is "likely" to fail the partition, untested); (iv) a **localised surface** — localisation is *emergent only semiclassically* (md:586-592, 658-659, 684-685), so near-`r_S` concentration is a downstream PREDICTION. Hayward-passing is **NECESSARY, NOT SUFFICIENT** for intrinsicality (the `relphi` precedent — passed global AUC, failed robustness).
- **Caveats:**
  - "Outward" in C1 must be an order-only past/complement cut; if it secretly tracks the EF in/out asymmetry (`generator.py:119-121`) or any interior/exterior label, it smuggles the forbidden outgoing spatial null direction (md:175).
  - C2's truncation confound is potentially **fatal, not merely controllable**: EGS md:193 shows future-cardinality diagnostics are boundary-dominated (flat diamond already `n`–`√n`). The MINK twin (`thresholds.py:51`) is necessary; stability of `κ` under varying the temporal cut and an intrinsic `E_indep` are additionally required, else reject.
  - C1's `β`-minimum may localise on the `r=0.1` near-singularity truncation or box walls rather than `r_S`; only the same-truncation MINK subtraction plus density-stability separates a membrane from an edge.
  - The Hayward sprinkling is *constructible* in 1+1D (md:157,166-169) but its physical viability is a **conjecture** in EGS (md:169); the 3+1 regular-BH partition failure is stated as "likely," not proven (md:195).
  - C3 (future width-collapse) is correctly REJECTED: a long-thin future is exactly the **singularity funnel**, absent in Hayward (md:195) — a warning that C2 must not covertly reduce to the same funnel.
  - `[UNVERIFIED]` Whether `β` (C1) or `κ` (C2), on the actual sealed cloud, peaks at `r_S` rather than the box boundary is unknowable without a run — out of scope (RESPECT_SEAL_FREEZE). The definitions are adjudicable as mathematics; their semiclassical correspondence is a prediction to be tested only through the frozen validation path, never tuned to pass.

## 5. Falsifier attack
*(Process note: this seat normally runs on Fable 5; Fable was unavailable this session, so it ran on **sonnet** as a model substitution — acceptable for a red-team pass, as in `comite_decision_004`/`005`.)*

**Concrete failure modes**
1. **C1 has no closed search procedure — the antichain class is unspecified.** `dev/X0_Qn_wellposedness_NOTES.md:647-649` says "∃ antichain A whose β(A) is a pronounced local minimum" but never specifies which antichains are searched, the ordering over them, or tie-breaks; "pronounced" is undefined. Confirmed by the mathematician. Promoting C1 with this gap means the predicate is parametrised by an unstated combinatorial search — a latent DOF fixable post-hoc after data are seen, violating `NO_POST_HOC_TUNING` (`docs/preregistration.md:55-57`). Not yet a closed falsifiable definition; a proof-of-concept sketch.
2. **C1's "outward" is not order-reduced.** "Outward causal bandwidth" (`notes:647`) framed as links from `down(A)` to complement; the word "outward" in the motivation (`notes:654-655`) still imports a spatial-directional intuition — the suppressed leg is the outgoing spatial null direction relphi/EGS needed an embedding for (`dev/INTRINSIC_OBSERVABLE_AUDIT_NOTES.md:354`). The reduction "outward = exiting the order ideal via a covering link" is asserted, not proven equivalent to suppressed outward-null flux without the embedding. Until that equivalence is a theorem, "outward" is a covert motivational handle that could select which of several tied-β antichains to report.
3. **C1 is vulnerable to the inextendible-antichain bypass (Surya "missed links").** If the search class is inextendible antichains, Surya proves an inextendible antichain misses infinitely many covering links in Minkowski — information "bypasses" the cut (`Directions_in_Causal_Set_QG_arXiv1103.6272.md:229-231`, Fig 6). So β(A) near the horizon is systematically deflated by missed-link leakage, NOT by trapping-specific suppression. The predicate could fire purely because a badly-placed antichain misses links for non-local reasons unrelated to trapping. If the search class is not restricted to avoid bypass-sensitive antichains, C1 is a leaky observable.
4. **C2's normaliser E_indep is not the right object for a wavefront antichain.** `notes:676-680` says E_indep is "supplied by the causet's own interval-abundance statistics (Benincasa-Dowker C_k)." But BD `C_k`/`N_m` count m-element order-intervals in a flat Alexandrov interval (`Glaser-Surya 2013 derived-md:36-44,189-196`). The common future `⋂J⁺(a_i)` for an m-element antichain is a completely different combinatorial quantity (not an order-interval). `⟨N_m^d⟩ ∝ (ρV)^{m+2}` is for "m-element diamonds between two fixed poles," not "intersection of future cones from m mutually incomparable elements." Using the former as the normaliser for the latter conflates two distinct invariants and re-imports a flat-Alexandrov object as the baseline for a curved-interior measurement — a covert embedding-specific calibration dressed as order-intrinsic.
5. **C2's truncation confound is potentially fatal and not discharged by the MINK control alone.** In `r∈[0.1,1.3]`, all futures eventually hit the `r=0.1` singularity truncation or the `t=6` wall; the common future is dominated by the smallest individual future, truncated by box walls. κ could move with the temporal cut even in the MINK control. A MINK twin with the same truncation is necessary but not sufficient: if the truncation artefact is amplified by the BH interior geometry (singularity shrinks interior futures more severely), `κ_BH>κ_MINK` measures singularity-truncation asymmetry — exactly the C3-failure mode already rejected (`notes:707-711`).
6. **Neither candidate has a closed threshold.** "β a pronounced local minimum" and "κ≫1" both contain unquantified comparators. Without a pre-committed threshold there is no falsifiable predicate — only a direction of search. The relphi precedent is directly on point (`audit notes:82,127-128`): relphi passed a global AUC yet failed robustness. A candidate lacking a pre-committed threshold can be "tuned to pass" after exploratory results — the exact failure `NO_POST_HOC_TUNING` prohibits.

**Ground-truth leakage**
- **C1 "outward" hazard:** the only way to decide which antichain minimises β is to scan the order; if the scan uses any coordinate-sorted traversal (index correlated with `r` via the sprinkling order), the hidden embedding guides which antichain is reported. Guard-v (checking the final observable is relabel-invariant) does NOT prevent this if the *search* for the minimising antichain is label-dependent — the argmin antichain could change under relabelling even if β(A) for a fixed A is invariant. The needed `tests/test_leak.py` extension does not exist yet (lines 27-41 cover the current volume estimator). Live gap.
- **C2 E_indep leakage:** if E_indep is calibrated from the same BH causet being tested, the normaliser already "knows" the geometry — absorbing the horizon focusing into the baseline. The definition does not specify whether the calibration comes from the test causet, a MINK twin, or a theoretical flat formula; the flat formula depends on dimension and density (not directly order-observable), re-importing the embedding at the calibration step.

**Freeze violations**
1. **β̄_bulk null-stat and E_indep baselines are not frozen.** No document freezes the MC seed for the β null over bulk antichains nor the formula/source for E_indep. Computing these from dev data before freezing, then choosing the threshold that "works," is first-order post-hoc tuning. `docs/preregistration.md`, `_002.md`, `estimator_v2_seal.md` are silent on C1/C2 (authored after those seals, at HEAD `aef48ce`).
2. **The dev probe `dev/measure_membrane_c1c2.py` is named in the dossier but does not exist** (`ls dev/` shows no such file). Producing results from a not-yet-committed script means the seal `6e2c3888` (covering `thresholds.py`) cannot attest to determinism; running an uncommitted script then freezing its outputs would violate the "seal pre+post" requirement.
3. **Search-procedure non-determinism** (REPRO `[UNVERIFIED]`): label-dependent tie-breaks are a hidden DOF; choosing the rule after seeing which gives "better" separation is post-hoc tuning not covered by Guard-v.

**Verdict coercion**
The five options put three promotion verdicts vs one REFINE / one REJECT — the framing asymmetrically favours promotion. The analyst's own note (`notes:11.5`) calls C1 "strongest candidate" and "ready for committee," an authorial nudge before adjudication. The independent-falsification gate (`preregistration.md:76-78`, "the author of a claim is never its sole verifier") is at issue: C1/C2 were authored in `dev/X0_Qn_wellposedness_NOTES.md` by the same analyst chain (HEAD `aef48ce`, §10→§11) that frames this committee. The falsifier is the only structural check; a fully independent pass (separate session, blind to §11) has NOT been done. Also: the dossier marks C2 "PASSES Hayward in principle iff the truncation confound is removed" — a conditional inside the PASSES classification. `PROMOTE_C1_WITH_C2_AS_INDEPENDENT_VALIDATOR` would use C2 (whose Hayward pass is conditional on a test not yet done) to validate C1 — two uncertain things cannot validate each other.

**Premature / over-broad claims**
- No prereg extends to C1 or C2; promoting either without a new sealed prereg would be an undocumented scope extension.
- The standing caveat (`notes:11.4,10.6`) is explicit that order-only + well-posed does not prove intrinsic-vs-semiclassical-shadow. Calling either a "genuine covariant order-theoretic precursor of the horizon" already over-claims: established is that they are *candidates* with order-only formulations passing a Hayward counterfactual.
- Event horizons require infinite sprinkling (EGS md:173); C1/C2 detect at most apparent/trapped regions in a finite patch — calling them precursors of "the horizon" risks conflating apparent and event horizons (`preregistration.md:69-72`).

**Independent-falsification gate**
Not satisfied. The C1/C2 candidates were authored by the same analyst chain now adjudicating them. The falsifier role is structurally distinct but invoked within the same session/tool chain that produced the dossier. A fully independent falsification — separate session, blind to the §11 analysis — has not been conducted. The gate would be satisfied only if this brief is treated as the independent pass AND the committee is barred from promoting without the falsifier failing to find a fatal flaw — which it does not: three open flaws are found (no closed search procedure for C1; E_indep category error for C2; neither threshold frozen).

**Minimal falsification test**
For **C1**: implement the antichain-scan as a concrete deterministic algorithm on two sprinklings (one BH, one MINK-twin, same truncation, same seed). Apply Guard-v's relabel-conjugation to the antichain-*selection* step specifically (not just to β(A) for a fixed A). If the selected minimising antichain changes under relabelling, Guard-v raises and C1 fails the leakage gate before any physics claim. Executable on DEV_SEEDS with no frozen-seed burn, using `tests/test_leak.py` infrastructure. For **C2**: compute κ on the BH and MINK-twin sprinklings with any concrete E_indep (e.g. flat BD at the same ρ,V). If `κ_BH≈κ_MINK`, the truncation confound swamps the signal → rejectable; if `κ_BH>κ_MINK`, record the temporal cut at which the gap first appears — if only near `t→6`, it is a boundary artefact. Both run on DEV_SEEDS, no commit/seal. If either fails, the correct verdict is `REFINE_CANDIDATES_BEFORE_PROMOTION`.

## 6. Pre-registration verdict
- **Verdict: PASS**
- **Freeze status:** No threshold exists to freeze for this step, and none is required. The question is a *conceptual adjudication of candidate predicates* — it fixes no numeric success criterion, no seed, no run protocol, no PASS/FAIL bar. All project thresholds remain frozen at prior seals: prereg-001 addendum, prereg-002 (`docs/preregistration_002.md:8`), prereg-003 (`docs/preregistration_003.md:9`), under seal `6e2c3888…`. "No threshold to freeze" is the honest answer, not a gap.
- **Seal integrity:** The step runs no sealed path, draws no seed, executes no `validate.run()`/`make dry-run`/`make gate`. Seal `6e2c3888…` confirmed at start and end of `dev/X0_Qn_wellposedness_NOTES.md` (header + §9 close) and matches `docs/preregistration_002.md:8`, `_003.md:9`. Working tree: four untracked dev notes only; `nachocausal/` 0 M. INTACT.
- **Seed discipline:** No seed drawn or consulted. `RESERVED_002` (2_000_000–2_999_999, `docs/preregistration_002.md:18`) virgin; `DEV_SEEDS`/`EXPLORE_POOL` untouched. A future dev probe would use `DEV_SEEDS`/`EXPLORE_POOL`, disjoint from `VALIDATION_SEEDS`. Virgin band not burned by conceptual deliberation.
- **Reporting rule:** Step explicitly reversible (`§11.5` "nothing is sealed or run here"). The five verdict options are symmetric outcomes enumerated in advance; report whichever the evidence compels. "PASS/FAIL/INCONCLUSIVE reported alike; no post-hoc anything" (`docs/preregistration_002.md:64-68`) applies by analogy.
- **Forbidden moves present?** The operative concern is the **latent post-hoc DOF freeze hazard** of promoting an *open* definition. **C1 hazard — REAL and BINDING:** "pronounced local minimum" (`§11.1`) has no search class, no threshold, no normalisation criterion; `|A|` is a free parameter. Fixing these after running on dev seeds violates `NO_POST_HOC_TUNING` (`docs/preregistration.md:55-57`) and `pr003_leakage_gate.md:65` ("Re-seleccionar / reajustar la regla después de revelar r"). **C2 hazard — REAL and BINDING:** `E_indep` is under-specified (`comite_decision_005.md:§4`; BD abundance is dimension/density-dependent, an interval count, not an m-antichain common-future abundance); specifying it post-hoc is a post-hoc normalisation. Both are order-only *as currently defined* (no leakage in the definition); the leakage/freeze hazard arises only if the underdetermined choices are fixed after seeing data. No reconstruction over-claim in the adjudication itself provided scope limits hold (1+1D, apparent/trapped, localisation emergent). No threshold loosening, no re-run-after-peek, no virgin-seed burn.
- **Reasons:**
  - **R1** — Promotion without closure is a latent post-hoc DOF, but the step itself is reversible dev deliberation committing nothing. PASS is conditional on the verdict being a development-phase decision with no code, threshold, prereg freeze, or sealed run. The freeze hazard becomes live when a dev probe is authorised and data seen — at which point C1's (search class, "pronounced") and C2's (`E_indep`) open choices **must already be frozen in writing** (`docs/preregistration.md:55-57`, `pr003_leakage_gate.md` contract #5).
  - **R2** — The pre-probe freeze discipline is a BINDING CONDITION on any PROMOTE verdict: whichever candidate, the committee must specify, before any probe, (C1) the antichain search class, the `β̄_bulk` normalisation, and a quantitative "pronounced" criterion (e.g. ">95th percentile of the MC null over same-|A| antichains, MC seed frozen in writing"); (C2) the exact `E_indep` for m-antichain common futures, the wavefront search class and size m, and the truncation-subtraction rule. Anchors: `docs/preregistration.md:49,55`; `pr003_leakage_gate.md:54-65`.
  - **R3** — The Hayward counterfactual and artefact vetoes are binding on any promoted candidate; "passes Hayward" is an analytic assertion (`§11.1/§11.2`), not a computed result. The intrinsicality caveat (`§11.4`) is not discharged by promotion.
  - **R4** — The prior prereg-002 PASS (`results/validation.json`) is not reopened, re-run, or touched. The new candidates are distinct from the sealed future-volume `O(i)` instrument (`docs/preregistration_003.md:§5`). No prior threshold loosened.
  - **R5** — Seal intact (`6e2c3888…`); `nachocausal/` unmodified; four untracked dev notes only. Within the permitted reversible dev-phase deliberation scope.

## 7. Literature verdict
| Citation | Claimed by | Status |
| --- | --- | --- |
| EGS 2605.06813 md:173 — event horizon requires infinite sprinkling | mathematician / physicist | CONFIRMED |
| EGS md:175 — futureless points at J⁺ indistinguishable from those near r=0 (even infinite causet) | physicist | CONFIRMED |
| EGS md:135 — induced 1+1D metric determinant constant (coordinate-uniform density) | physicist | CONFIRMED |
| EGS md:193 — future-cardinality boundary-dominated; Minkowski diamond gives cardinality n–√n | physicist | CONFIRMED |
| EGS md:195 — futureless-point partition "likely" fails for regular (Hayward) BHs in 3+1D | physicist | CONFIRMED |
| EGS md:199 — apparent horizon remains applicable where the partition fails | physicist | CONFIRMED |
| EGS md:201 — apparent horizon = marginally outer trapped surface | physicist | CONFIRMED |
| EGS md:157,166-169 — Hayward sprinklings constructible; md:169 stated as a conjecture | physicist | CONFIRMED |
| EGS md:208,217,223-225,230,282 — Θ=(1/A)dA/dλ (Eq.11), Θ_out=0 at r=2M, discrete expansion | mathematician / physicist | CONFIRMED |
| EGS md:142-145 — outgoing geodesic direction requires embedding (t_*out) | physicist | CONFIRMED |
| Surya LRR :882-885 — inextendible antichain A separates C=Fut(A)⊔Past(A)⊔A | mathematician | CONFIRMED |
| Surya LRR :886 — "missing link": Past element can link to Fut element bypassing A (leaky cut) | mathematician / falsifier | CONFIRMED (load-bearing for "C1 not a closed definition") |
| Surya LRR :1875-1920 (eq.51) — ⟨N^d_m⟩ interval abundance dimension- & density-dependent (∝ e^{−ρV}); counts order intervals, not common futures | mathematician / falsifier | CONFIRMED (load-bearing for "C2 E_indep is a category error") |
| BD 2010 (1001.2725) md:40 — link = I(x,y) contains only x,y; md:123 — N_i = (i+1)-element inclusive intervals | mathematician | CONFIRMED |
| `Directions…1103.6272` :229-231, Fig 6 — inextendible antichain misses infinitely many links in Minkowski | falsifier | CONFIRMED |
| Myrheim 1978 (CERN TH-2538) — general causet anchor | mathematician | UNVERIFIED (no standalone PDF; cited only within Surya LRR + Bombelli 1987) |
| Bombelli 1987 PhD — general causet anchor | mathematician | CONFIRMED (PDF + derived-md present) |

- **Notes:** All EGS line claims verified precisely (the "likely fails" md:195 and the Hayward conjecture md:169 are accurately hedged by the experts). The two load-bearing "neither is closed" findings are both grounded: Surya :886 explicitly names the leaky-cut defect (C1), and Surya eq.51 / BD :123 confirm the abundance baseline counts *order intervals*, NOT m-antichain common futures (C2's `E_indep` category error). Myrheim 1978 is the only UNVERIFIED item and is non-load-bearing (general anchor, accurately described in secondary sources).

## 8. Synthesis
**Convergent committee position: `REFINE_CANDIDATES_BEFORE_PROMOTION`.** Three roles independently converge: **neither C1 nor C2 is yet a *closed* falsifiable definition**, so promoting either as the project's horizon observable is premature.

**On the user's critical "outward" objection (resolved).** The mathematician and physicist agree the *partition* `down(A)` (the generated order ideal) vs its complement is **genuinely order-canonical — no smuggled exterior** (Surya :882-885). The future/past direction is intrinsic to the poset; it is NOT the forbidden spatial outgoing-null direction. **However**, the word "outward" and the membrane motivation must be **reduced in writing** to "forward cover-flux escaping the order ideal `down(A)`," and the search/selection of the minimising antichain must itself be relabel-invariant (Guard-v applied to the *argmin*, not only to β of a fixed A) — else label-dependent tie-breaks or a coordinate-sorted scan re-introduce the embedding (falsifier ground-truth-leakage). So the objection is **valid in spirit and dischargeable by refinement**, not fatal.

**Why C1 is not yet closed.** (i) No antichain search class is specified and "pronounced local minimum" is undefined as an order functional (mathematician; falsifier #1). (ii) The inextendible-antichain cut is **leaky** — Surya :886 / `1103.6272`:229-231 confirm it misses links — so β can be deflated for non-trapping reasons and is sensitive to which antichain is chosen (falsifier #3). (iii) No frozen threshold / `β̄_bulk` MC seed (falsifier #6, freeze-violation #1). These are **fixable by closing the definition**, not fundamental defects — C1 remains the strongest, most refinable candidate (its normalisation is order-intrinsic by construction; the physicist confirms its membrane target passes Hayward in principle).

**Why C2 is not yet closed.** Its raw functional `⋂J⁺` is clean and computable, but **`E_indep` is a category error** (load-bearing, lit-confirmed): the Benincasa–Dowker abundance counts *order intervals*, dimension- and density-dependent, NOT the common future of an m-element antichain (mathematician; falsifier #4). And the **truncation confound is potentially fatal** (EGS md:193 boundary-dominated cardinality): the MINK twin is necessary but insufficient unless `κ` is stable under moving the temporal cut and `E_indep` is intrinsic (physicist; falsifier #5). C2 needs a genuinely new m-antichain common-future null before it is even well-posed.

**Open disagreements (not hidden).**
1. **User's prior vote (`PROMOTE_C1_WITH_C2_AS_INDEPENDENT_VALIDATOR`) vs committee (`REFINE`).** Resolved by sequencing, not contradiction: the C1-event / C2-validator *architecture* is sound in principle (and matches the mathematician's read that C1 is the stronger axis), but **cannot be enacted while both are open** — the falsifier's point is decisive: C2's Hayward pass is *conditional* on a test not yet done, so "two uncertain things cannot validate each other." Promote only after C1 is closed AND C2 is re-posed with an intrinsic `E_indep`. The user explicitly conditioned the vote "subject to the outward audit"; the audit returns: refine first.
2. **Which candidate is closer to promotable.** Mathematician: C1 (intrinsic normalisation, orientation-clean once "outward" is fixed) over C2 (closed raw statistic but weakest normaliser, most truncation-exposed). The committee adopts C1 as the **primary refinement target**, C2 as a **secondary** one to re-pose.
3. **Independent-falsification gate.** The falsifier flags that the candidates were authored by the same analyst chain now adjudicating them, within one session. This does not block a REFINE verdict (REFINE commits nothing), but it is **binding before any future promotion**: a fresh, blind independent pass must try to break the *closed* definitions.

There is **no pre-registration BLOCK** (warden PASS) — but there **is** an unresolved set of definitional gaps that prevents a PROCEED-to-promotion verdict. Hence the committee recommends revise-and-reconvene.

**Recommended direction:** record `REFINE_CANDIDATES_BEFORE_PROMOTION`; take the reversible analytic refinement of C1 (and re-posing of C2) per §9; do NOT promote, freeze, probe, or commit. Ranked alternatives: (B) `PROMOTE_C1_WITH_C2_AS_INDEPENDENT_VALIDATOR` now — **rejected** (open definitions = latent post-hoc DOF; C2 validator-role unusable while conditional). (C) `PROMOTE_C1_AS_PRIMARY` now — **rejected** (same closure gap; no frozen threshold/search class). (D) `REJECT_BOTH_AS_NOT_INTRINSIC` — **rejected** (the flaws are fixable; C1's partition is genuinely intrinsic; the line is alive). (E) `PROMOTE_C2` — **rejected** (weakest of the two; `E_indep` not even well-posed).

## 9. Next-step spec
**Reversible steps (analytic; may be done now if the user asks — no code, no seed, no sealed path, no commit). Work stays in `dev/X0_Qn_wellposedness_NOTES.md`.**
1. **CLOSE C1 in writing (primary).** (a) Replace "outward bandwidth" with **"forward cover-flux escaping the order ideal `down(A)`"** — purely order-defined, no spatial direction. (b) Fix a **closed antichain search class** on principled order-theoretic grounds (e.g. maximal antichains, or size-`k` antichains for a pre-declared `k`), chosen NOT from data. (c) Make **"pronounced local minimum"** a quantitative order functional with a pre-committed comparator (e.g. `Δ(A)` beyond a stated percentile of the self-calibrated `β̄_bulk` null, MC seed declared in writing). (d) Address the **leaky-cut** (Surya :886): restrict the search class to bypass-robust antichains and/or define the flux to count *all* separating relations crossing (not only covers), and state the robustness of `Δ` across the antichain family. (e) Require the **argmin/selection step itself to be relabel-invariant** (Guard-v on selection, not only on β).
2. **RE-POSE C2 in writing (secondary).** Define `E_indep` as a genuine **m-antichain common-future null** — intrinsic, pre-specified, parameter-free — NOT the BD interval abundance (which is the wrong object). Fix the wavefront search class and size `m`. Specify the **truncation control**: MINK twin, same cloud, same truncation, AND an explicit **`κ`-stability-under-temporal-cut** requirement (reject if `κ` moves with the cut).
3. **(Optional, only if the user authorises a dev probe — still reversible) the falsifier's minimal test.** On `DEV_SEEDS`/`EXPLORE_POOL` (disjoint from `VALIDATION_SEEDS`), one BH + one MINK-twin (same cloud, same truncation): for C1, apply Guard-v relabel-conjugation to the antichain-*selection* step (argmin changes under relabel ⇒ C1 fails the leakage gate before any physics claim); for C2, compute `κ_BH` vs `κ_MINK` and the temporal cut at which any gap first appears. Seal asserted pre+post, numpy 1.26.4, MC null seeds frozen+logged, results NOT written to the sealed `results/` validation path, no commit. A new `dev/measure_*.py` would be needed (does not exist yet).

**Committing steps (NONE without explicit user authorisation):** no promotion of either candidate as the project axis; no prereg; no freeze of search class / threshold / `E_indep` / MC seed; no sealed run; **no commit of the dev note as a result**. **RECONVENE** the committee to promote only after (i) C1 is CLOSED in writing (and C2 re-posed), and (ii) an **independent pass blind to the §11 authoring** has tried and failed to break the closed definition (satisfies the independent-falsification gate, `preregistration.md:76-78`).

**Binding rules pre-committed:** `RESPECT_SEAL_FREEZE` (`6e2c3888…`, re-verify before/after any artefact); `NO_GROUND_TRUTH_LEAKAGE` (β/κ AND the antichain *selection* must be relabel-invariant — Guard-v extended to selection; coordinates only ever score, never define/select); `NO_POST_HOC_TUNING` (C1 search class + "pronounced" comparator + `β̄_bulk` MC seed, and C2 `E_indep` + search class + size `m` + truncation rule, ALL frozen in writing BEFORE any dev seed is scored); `NO_RECONSTRUCTION_CLAIM` (at most a relational apparent/trapped transition in a finite 1+1D patch — never event horizon, never 3+1D, never a localised surface; localisation is a downstream semiclassical prediction); Hayward counterfactual + flat-control separation + truncation-subtraction binding on any promoted candidate; `NO_THRESHOLD_LOOSENING` (no frozen threshold touched). The standing intrinsicality caveat stays active: order-only + Hayward-passing is NECESSARY, not SUFFICIENT.

**Minimal falsification test (carried forward):** §5's — for C1, Guard-v on the antichain-selection step (label-dependent argmin ⇒ fail before any physics); for C2, `κ_BH≈κ_MINK` or a gap appearing only near the temporal wall ⇒ reject. Both runnable on `DEV_SEEDS` with no frozen-seed burn and no commit; either failure confirms `REFINE_CANDIDATES_BEFORE_PROMOTION`.

## 10. Verdict
COMMITTEE_DECISION_VERDICT=RECOMMEND_REVISE_AND_RECONVENE

## 11. User sign-off
_(left blank for the user — decision, date, and any overriding notes)_
