# Comité Decision 044 — Revisión de la adjudicación C6 (`INTERNAL_ALEXANDROV_WAIST_SCREENS`, decision 043)

> Produced by `/comite`. The committee PROPOSES; the user AUTHORISES. The committee never executes
> a one-way or outward-facing action (the blind validation run, a commit/push, anything
> irreversible), never tunes a frozen threshold post-hoc, and never makes a reconstruction claim.
> Guardrails: `NO_RECONSTRUCTION_CLAIM`, `NO_POST_HOC_TUNING`, `NO_THRESHOLD_LOOSENING`,
> `NO_GROUND_TRUTH_LEAKAGE`, `RESPECT_SEAL_FREEZE`.

## 1. Decision question
Revisar la adjudicación conceptual C6 (`INTERNAL_ALEXANDROV_WAIST_SCREENS`, objeto
`W(p,q) = {x ∈ C : p ≺* x ∧ x ≺* q}`) redactada en
`docs/comite/comite_decision_043_c6-internal-alexandrov-waist-screen-adjudication.md`, con terminal
propuesto `C6_BLOCKED_NO_INTRINSIC_SCREEN_TRANSPORT`. ¿Es válida la prueba de antichain, correcto el
objeto y su acotación física, honrada la disciplina (sello/freeze/prohibiciones de decision 042), y
es el terminal el negativo honesto por precedencia?

## 2. Verified state
Hechos comprobados **esta sesión**, cada uno con su comando / `file:line`. Lo no comprobado se marca
`[UNVERIFIED]`.

- **Working tree:** `git status --short` → único cambio `?? docs/comite/comite_decision_043_c6-internal-alexandrov-waist-screen-adjudication.md` (untracked). `HEAD = 050a4e2`. `git diff --stat HEAD` sobre `thresholds.py` y docs de sello/prereg: **cero cambios** (confirmado por reproducibility + warden).
- **Seal:** `sha256sum thresholds.py = 6e2c38881234cef48e859096b46f261cfa83ea8a2f6c955cc1dbc42537bfefd4`, **bit-idéntico** al sello congelado en `docs/preregistration_002.md:7-8` y `docs/preregistration_002_result.md:21,80` (prereg-002 seal #3, commit `573cfcb`). Sello intacto; 043 no ejecuta ningún path sellado. `RESPECT_SEAL_FREEZE` honrado.
- **Ancla — relación de enlace `≺*`:** reducción transitiva `L = C & ~((C·C)>0)` en `dev/measure_kbeam_peeloff.py:111` (asignaciones reales GPU `:121`, CPU `:130`, CSR `:131-137`; docstring `:29`). Confirmado por mathematician, reproducibility y literature verifier (drift de línea inmaterial).
- **Ancla — intervalo cerrado:** `|[e,f]|` incluye ambos extremos, `dev/PR009_LADDER_ENSEMBLE_EFFECTIVE_EXPANSION_PREREGISTRATION.md` §5.3 (`:102-104`).
- **Ancla — cierre C1–C5:** `docs/comite/comite_decision_042_c1-c5-localizer-line-closure.md`: línea de localizadores cerrada; `C5_F3 = FAIL_WALL_BRIDGE_TWIN_AMBIGUITY`; §6 (`:188-192`) prohíbe reabrir CANDIDATE_5/C4 y "arreglar F3" con ε-clustering o grafo de vecinos no resuelto.
- **Ancla — C4 scoped:** `docs/comite/comite_decision_039_c4-neighbor-graph-adjudication.md:384-401` — el rechazo de C4 está **acotado a dos familias auditadas** y es explícitamente "**not a theorem** that no order-only spatial adjacency can ever be defined on any causet", y no refuta métodos "2-link".
- **Ancla — Lean:** `formal/HorizonFormal/HorizonFormal/Horizon.lean:56-57` define `IsCover x y := x<y ∧ ¬∃z, x<z<y`; `grep` sobre `.lean` del proyecto (excl. mathlib) → **cero** objetos `W`/`Waist`/`Alexandrov`/lema antichain. La antichain de 043 es teorema en prosa, **no** machine-checked.
- **Ancla — PR009:** `dev/PR009_LADDER_ENSEMBLE_EFFECTIVE_EXPANSION_CLOSURE_DECISION.md:3-4,13-20,52-61` — `TERMINAL_LABEL: FAILED_DATA_CONTRACT`, cierre **previo al scoring**, "does not establish … a null or negative physical result". No es negativo físico contra esta línea.
- **Alcance:** 043 declara `NO_IMPLEMENTATION / NO_SYNTHETIC_EXECUTION / NO_SEEDS / NO_FREEZE / NO_CONTRACT`; ninguna semilla consumida, ningún poset sintético, ningún path sellado ejecutado. Revisión doc-only, no committing.

## 3. Dossier
Ficheros y referencias suministrados al comité:
- `docs/comite/comite_decision_043_c6-internal-alexandrov-waist-screen-adjudication.md` (objeto de revisión)
- `docs/comite/comite_decision_042_c1-c5-localizer-line-closure.md` (§6 prohibiciones, §7 menú C6)
- `docs/comite/comite_decision_039_c4-neighbor-graph-adjudication.md` (`:384-401` scoping)
- `dev/PR009_LADDER_ENSEMBLE_EFFECTIVE_EXPANSION_{PREREGISTRATION,CLOSURE_DECISION}.md`
- `dev/measure_kbeam_peeloff.py` (`gpu_link_csr`, `:111`)
- `formal/HorizonFormal/HorizonFormal/Horizon.lean` (`IsCover`, `:56`)
- `docs/claim_grammar.md` §3; `docs/preregistration_002.md` + `_result.md`; `docs/estimator_v2_seal.md`
- `biblioteca/derived-md/Towards black-hole horizons and geodesic focusing in causal sets.md` (EGS §§II, IV)
- `biblioteca/derived-md/Benincasa_Dowker_2010_*` y `Benincasa_Dowker_Glaser_Actions_Quantum_Counting_arXiv2505.22217.md`
Guardrails vinculantes: `NO_RECONSTRUCTION_CLAIM`, `NO_POST_HOC_TUNING`, `NO_THRESHOLD_LOOSENING`, `NO_GROUND_TRUTH_LEAKAGE`, `RESPECT_SEAL_FREEZE`.

## 4. Expert briefs (wave 1 — blind, parallel)
### Reproducibility engineer brief
- **Proposed artefact(s):** Single untracked file `docs/comite/comite_decision_043_c6-internal-alexandrov-waist-screen-adjudication.md`. Confirmed the sole repo change: `git status --short` shows only `?? docs/comite/comite_decision_043...md`; `git diff --cached --stat` and `git diff --stat` are both empty. HEAD=`050a4e2`. §19 self-attests "único archivo de la tarea… no hay commit ni push". No code, no `dev/` script, no `thresholds.py`, no manifest/ledger touched.
- **Environment & seal:** Seal re-verified independently: `sha256sum thresholds.py` = `6e2c38881234cef48e859096b46f261cfa83ea8a2f6c955cc1dbc42537bfefd4`, bit-identical to the DOSSIER value → `thresholds.py` untouched, `RESPECT_SEAL_FREEZE` honoured. Package-diff clean: 043 introduces no import/dependency/env reference; the external-Minz/`venv_minz` dev-run path in `CLAUDE.md` is not invoked. The second `thresholds.py` under `.public_export_staging/` (SHA `2b4c90…`) is unrelated and untouched.
- **Provenance capture:** The doc records entry-state tokens, DATE, and a terminal block (§18, §19). Anchors use `path:line`; re-verified the load-bearing one: `dev/measure_kbeam_peeloff.py:111` `gpu_link_csr` genuinely computes `≺*` as `L = C & ~((C·C)>0)` (GPU `:121`, CPU `:130`, CSR `:131-137`). The closed-interval anchor confirmed at `dev/PR009_..._PREREGISTRATION.md:102-104`. §16 budget structures (`C`, `L`, `gpu_link_csr`) are real and correctly named.
- **Run mechanics:** No-op / fully reversible, non-committing. No `Makefile` target invoked; the sealed PASS/FAIL path is never touched. Declared invariants `NO_IMPLEMENTATION / NO_SYNTHETIC_EXECUTION / NO_SEEDS / NO_FREEZE / NO_CONTRACT` honoured by the observed tree. `NO_POST_HOC_TUNING`/`NO_THRESHOLD_LOOSENING`/`NO_GROUND_TRUTH_LEAKAGE` non-engaged (no data, no threshold, no embedding read).
- **Reproducibility risks / ambiguities:**
  - Anchors are line pins (`:111`, `:104`) but the doc does **not** embed the HEAD SHA (`050a4e2`) or seal SHA inside its own text — line anchors can drift on future commits. Minor hygiene gap. `[low severity]`
  - §16's complexity figures (`O(N³)` family build, `O(N⁴)` transport, `O(N³)` for `C·C`) are naive-worst-case bounds, explicitly qualitative ("sin tiempos inventados") — sound, no fabricated timings. `[anchored, §16]`
  - §16 "columna de link-hijos de `p` … fila de link-padres de `q`" maps cleanly onto the code (CSR link-children `:131-132`; link-parents = row-slice of dense `L_host`, `O(N)` bitset) — accurate, no false claim of a pre-existing parent CSR. `[anchored, :131-137]`
  - IF a future C6 were opened, the doc's analysis implies (but correctly does not commit): a compute-budget cap + frozen `UNCOMPUTABLE` terminal before any `O(N⁴)` run; deterministic committed synthetic contracts with pre-registered seeds and pre-validation thresholds for §11 falsifiers; a relabel-invariant tie-break spec (the very object C4/C5 proved nonexistent).
- Net: 043 carries no committing/reproducibility footprint, §16 budget anchored to real structures, seal/dev-validation separation untouched, `NO_IMPLEMENTATION/NO_SEEDS` honoured. Only defect: no embedded commit/seal SHA.

### Mathematician brief
- **Computability:** All predicates decidable on `≺`/`≺*` alone. `≺*` is the frozen transitive reduction (`dev/measure_kbeam_peeloff.py:111-137`; matches EGS md:100). `W(p,q)` is the intersection of column-`p` link-children with row-`q` link-parents of `L` — boolean slicing, `O(N)` per pair. `P_int` is a first-order formula over `≺`, decidable and relabel-invariant. The abstain gate `W=∅` (§4.7, §11) is correctly a *non-admission*, not "area zero". Decidable order-only: CONFIRMED.
- **Order observable:** `W` is exactly the set of *midpoints of length-2 link chains* `p≺*x≺*q`, a proper subset of the open Alexandrov interval `(p,q)` (§4.2 valid). Choosing `W` over `(p,q)` is justified: `(p,q)` is generically not an antichain; `W` provably is (§5). Structural consequence: `W≠∅` requires `p,q` at **link-distance exactly 2**; for any taller diamond `W=∅`. So `|W|` is an abundance of 2-link bridges, **not** a codim-2 waist of a general diamond. The doc correctly refuses codim-2/area attribution (§6, §8, §17). `|W|` carries no established area content order-only.
- **Antichain proof audit:** VALID and rigorous under the exact repo convention. `x,y∈W, x≺y ⟹ p≺*x⟹p≺x`, with `x≺y` gives strict chain `p≺x≺y` (strict since irreflexivity forces `x≠p`, hypothesis `x≠y`), contradicting `p≺*y` a cover. Symmetric branch via `x≺*q` (`x≺y≺q`) independently closes it. Uses only "≺* = transitive reduction ⟹ no strict intermediate", the frozen def at `:111`; no embedding, no interval cardinality. Incomparable `x‖y` needs no handling. **No gap.**
- **Relevant invariants:** (i) height/longest-chain — same order-only diagnostic EGS uses to split minimals (EGS md:181,188,191); the optional `height⁻(p),height⁺(q)≥h` gate (§7) is well-anchored. (ii) Interval-cardinality abundances (Benincasa–Dowker) are the standard manifold/curvature invariants; `|W|` is **not** one and inherits their density/dimension confounds (§8, §14). (iii) `|W|` is a genuine new relabel-invariant order-invariant but with no established geometric meaning — Gate-2 verdict ("closed-provisional, not physical") is correct.
- **Transport completeness:** The no-closed-transport conclusion is order-theoretically sound *relative to the stated closure criterion* (single-valued/identity-preserving, relabel-invariant, threshold-free, internal to `{W(p',q')}`). Mechanisms A–D exhaust the natural constructions; each fails as claimed. Coordinated two-sided advance requires a bijection `W(p,q)↔W(p',q')` = a relabel-invariant lateral neighbor pairing — the object decision 039 proved nonexistent and 042/C5 could not supply. That reduction is a legitimate order-theoretic argument, not a rename. **One candidate the doc does not explicitly enumerate:** an **antichain push-forward** (minimals of `∩_{w∈W}J⁺(w)`, or covers of `W`) — order-only and well-defined, but (a) generically ≠ any `W(p',q')`, exiting the family, and (b) asserting it *is* the same screen still needs an element-identity map = the same missing pairing. Fails for the identical reason; no genuine gap. **Honest scope note:** the terminal is **not** a theorem that no order-only relation exists — a branching relation (mechanism D) trivially exists; it is the nonexistence of a *canonical/identity-preserving* one, which is precisely what `NO_INTRINSIC_SCREEN_TRANSPORT` names. Complete as stated.
- **Caveats:**
  - The impossibility is **criterion-relative** (canonical/single-valued), not absolute; doc explicit (§9.4-9.5). Endorse the terminal *as scoped*.
  - `W≠∅` ⟺ link-distance-2; `|W|` measures 2-link-bridge abundance, not a general-diamond waist. Risk 1–2/§7 register the "almost-always ∅/singleton" hazard.
  - Convention landmine (does not affect 043): EGS Def. 1 cond. 3 `|[p_i,q_j]|=2(j-i)` uses an **exclusive/open** interval count (EGS md:238-241,249), whereas repo/PR009 §5.3 closed `|[e,f]|` **includes endpoints**. 043 defines `W` through `≺*`, never through interval cardinality, so it is immune — but any future step importing EGS's `=2(j-i)` into repo closed-interval code would be wrong.
  - `|W|` density/interval-size confounds (§8) are real; §10 refusal to assign a sign order-only is warranted.
  - The transport→C4/C5 reduction is a *heritage* argument, not a reopening of C5 (§12 uses none of the prohibited minimal-partition inputs).
- Assessment: antichain proof valid, object well-posed and decidable, no-transport complete **as scoped** — terminal `C6_BLOCKED_NO_INTRINSIC_SCREEN_TRANSPORT` is order-theoretically supported.

### Mathematical logic brief
- **Formal status:**
  - *Antichain (§5):* **PROVED THEOREM** (order-theoretic, valid; holds in any poset under the cover relation). Foundation = repo frozen link def (`:111`), independently Lean-formalized as `IsCover` (`formal/HorizonFormal/HorizonFormal/Horizon.lean:56`). Caveat: the lemma **itself is NOT machine-checked** — no `W`/`Antichain`/`Waist`/`Alexandrov` object in project Lean. Correct *prose* theorem, not verified.
  - *|W| closed (§8):* DEFINITION + trivial relabel-invariance lemma. "Provisional area" is explicit **interpretation/future-hypothesis**, disclaimed as not physical/not codim-2 (§6, §8).
  - *No closed transport (§9):* **NOT a proved non-existence.** An **∃-refutation of a finite menu** (A–D) plus an *asserted* common obstruction — conjecture strength.
  - *No sign of ΔA (§10):* CONDITIONAL corollary of the transport conjecture; inherits its non-theorem status.
- **Quantifier / dependency order:** No-transport is **∃-refutation-of-a-menu dressed as ∀-non-existence.** §9 refutes four mechanisms; §9.5/§18 generalize to "no existe un transporte order-only cerrado" — the universal quantifier over *all* order-only rules is **never discharged** ("obstáculo común es estructural" is asserted). Honest reading: *no closed transport among the four examined mechanisms.* Gate-1 precedence: "family exists" is a **decidable** `≺`-predicate (`P_int`, genuine non-emptiness via §5); "minimal screen interpretation exists" is a **judgement**, not decidable (§6 frames it as interpretation). The decision procedure smuggles one interpretive step exactly where it picks rule 2 over rule 1. Since the doc states codim-2 is **undemonstrated** (§6, §17.1), if rule 1's "screen" were read at face value it would **fire** → `NO_STABLE_CODIM2`. The two BLOCKED terminals coincide only in polarity; the specific terminal rests on reading rule-1's "screen" at interpretation-strength — a real definition-scope slip (does not flip the sign).
- **Equivalence claims:** The reduction "symmetric transport ⟹ intrinsic lateral pairing = C4/C5 impossibility" is **rigorous ONLY for mechanism A's symmetric-endpoint variant** (§9.1). As a **general** reduction it is **asserted, not proved** (§9.5/§12). Compounding: the inherited impossibility is itself **scoped, not universal** — decision 039 states verbatim it is "not a theorem that no order-only spatial adjacency can ever be defined on any causet" (`039:400-403`). Chain = an *asserted* implication ∘ a *scoped* impossibility; neither link is a hard theorem, so their composition cannot be a proved non-existence.
- **Type / object discipline:** `W(p,q)` handled consistently as a **SET** (antichain), not conflated with a physical surface, ideal, or equivalence class; §6 withholds surface/codim-2/marginal/area status; `|W|` kept as cardinality. `P_int` a class of pairs, `W` a set of sets — no slip. "Screen"/`pantalla` oscillates between *defined object* and *interpretive label* but the doc flags the boundary (§6); the oscillation only bites at Gate-1/rule-1 precedence.
- **Caveats:**
  - Antichain theorem valid in prose but **unverified in Lean** (project Lean formalizes only `IsCover`).
  - §18 one-line summary overstates a finite-menu refutation as universal non-existence; honest scope = "no closed transport among A–D + asserted obstruction".
  - The reduction's universal step and the inherited C4 impossibility are **both non-theorems** (039:400-403 explicitly scoped) — the no-transport terminal is **conditional/conjecture-grade**.
  - Gate-1 non-firing of rule 1 depends on an interpretive "minimal screen" judgement (sound on decidable `P_int≠∅`, judgement-laden on "screen"). Negative polarity unaffected.
  - No guardrail breach; seal/thresholds untouched.
- Recommended framing correction: relabel the terminal-supporting claim as "no closed order-only transport **among examined plausible mechanisms**", and mark the transport→lateral-pairing reduction as proved-for-mechanism-A / conjectured-in-general.

### Physicist brief
- **Coordinates & patch:** Finite 1+1D Schwarzschild patch; EGS sprinkle the induced (t*,r) metric with constant determinant → constant density (EGS md:130,135); ladders are null-geodesic tracers (Def. 1, md:245). Decisive fact 043 rests on (EGS §IV): *"we do not have spatial two-surfaces available … we consider the one-dimensional spatial distance between neighboring geodesics"* (md:227-230). A codim-2 surface in 1+1D is 0-dimensional; EGS forfeit the 2-surface. So the doc's caution "codim-2 interpretation may lack support" (§6, §15 risk 10) is **physically correct**, not merely conservative.
- **Physical meaning of the signal:** EGS's area A in Θ=(1/A)dA/dλ (eq. 11) is the cross-section of a congruence traced along the affine parameter; the discrete counterpart (eq. 14) is a ladder-**ensemble** transverse-distance observable measured along a ladder **with identity across depth**. `|W|` is the cardinality of a *single* interval's internal antichain — a different object, plausibly density/scale-dominated, not area-like (§8 concedes growth with ρ and with `|[p,q]|`). §13 is correct: EGS obtains expansion along an *already-traced* trajectory, so EGS focusing does not by itself supply transport of an antichain-waist.
- **Sprinkling domain / confounds:** Two confounds correctly identified and unresolved: (i) density (`|W|` rises with ρ), (ii) interval size (§8, §9.3, §11.4,7). Even EGS's own discrete expansion recovers only the **sign** of Θ, and only after averaging over sprinklings/locations (md:344-347) — a per-realization order-only expansion is not claimed even by EGS. Truncation/ceiling dependence real (§11.5, §15.7), appropriately flagged.
- **Claim boundary:** Correct and correctly bounded. The doc forbids as *results* codim-2, marginal surface, geometric waist, physical area=|W|, expansion, trapping (§6, §10, §18). Marginality properly unreachable: EGS's apparent horizon is a *marginally outer* trapped surface (Θ_out=(1/r)(1−2M/r)=0 at r=2M, eq. 12) needing an outer/inner congruence distinction `W` has no order-only access to. On the two congruences: EGS's ingoing/outgoing belong to the **same** surface; §10 correctly argues advancing p vs q are the two *corners* of one interval (translation+scale mixed), NOT ingoing/outgoing → no order-only sign of ΔA. PR009 beam non-reuse correct (its beam is a population summary of a ladder ensemble, distinct from `W`); PR009 closure verifiably `FAILED_DATA_CONTRACT` before scoring, not a physical negative. 1+1D-crossing non-generalization flagged (§13, §15.9).
- **Caveats:**
  - Link relation cited is the frozen transitive reduction (`:130`, inside `gpu_link_csr` `:111`) — matches §4. Verified.
  - Antichain proof (§5) uses only the covering relation; valid, no embedding leakage.
  - Gate 1 "abundance plausibly HIGH, O(N²)" (§7) is an *unexecuted* heuristic; dual risk (almost always empty/singleton) explicitly retained (§15.1-2). Bounded conjecture, correctly not scored. `[UNVERIFIED — untestable without execution]`
  - EGS eq. (12) sign change at r=2M is a **continuum** statement; the doc never imports it as a discrete/order-only result.
- Assessment: physics claim boundaries correct and correctly bounded against EGS §IV; terminal rests on genuinely physical grounds. **Endorse.**

## 5. Falsifier attack
### Falsifier attack
- **Concrete failure modes:**
  1. **Gate-1 "abundant" claim is unsupported and self-contradicted.** §7 (`:192-196`) asserts "Abundancia … Plausiblemente ALTA … una fracción [de O(N²) pares] tendrá W≠∅," yet §4 point 6 (`:111-114`) states `W(p,q)` is non-trivial **only** when `p≺q` is realized "a distancia-de-enlace 2". The number of length-2 link-paths through a transitive-reduction DAG (`L = C & ~((C·C)>0)`, `:111`) is bounded by the sum of squared link out-degrees, generically `O(N)`–`O(N log N)`, **not** a "fraction" of the `O(N²)` internal-pair population. The doc's own risk register (§15.1-2 and §7: "no puede resolverse sin ejecución y queda como riesgo abierto") admits abundance is unresolved — **directly contradicting** the "ALTA" language two paragraphs earlier. Internal inconsistency, not merely a caveat.
  2. **Mechanism A's "inherited impossibility" from C4/C5 is an unproven analogy.** §9.1/§12 (`:380-385`) assert the lateral pairing "es exactamente el mismo objeto" that Decision 039 showed inexistent. But 039's negative (`:394-403`) is explicitly scoped to "the **two audited families** … It is not a theorem … does not refute … `2-link` methods." `W(p,q)`'s A-mechanism pairing (link-children of `p` vs link-parents of `q`, on an arbitrary interval, not `Min(C)`) is neither Rideout-Wallden nor Boguñá-Krioukov, and is arguably closer to the "2-link methods" 039 declines to refute. 043 asserts identity of object rather than demonstrating a reduction. Mechanisms B–D fail independently on their own (real) grounds, so the terminal likely survives without A — but the rhetorical anchor "reinforces the negative … inherited from C4/C5" (§12, §18) over-claims the strength of that inheritance.
  3. **§18's summary drops the mechanism-scoping qualifier.** §9 (`:306`) and §18 (`:489-491`) state flatly "no existe un transporte order-only cerrado" — unqualified — when what was shown is "none of A–D closes." An ∃-refutation over a 4-item menu reported with ∀-strength language.
- **Ground-truth leakage:** Non-engaged. `W(p,q)`, `≺*`, height, the optional `h`-threshold are all defined purely from `≺`; no coordinates/density/embedding define the object or the (failed) transport. Doc-only/no-execution → no scored artifact to leak. Clean.
- **Freeze violations:** Non-engaged for the primary vectors — only the untracked doc; `thresholds.py`/seal untouched; no seeds/code/manifests. One minor procedural gap (not a violation): no inline HEAD/seal SHA (anchor-drift risk).
- **Verdict coercion:** Real tension, not a smoking gun. §6 disclaims codim-2 ("Ninguna atribución de codim-2 … se toma como establecida") yet the same Gate-1 verdict (§7, `:201-206`) treats the bare antichain as a sufficient "interpretación mínima de pantalla" to avoid firing terminal (1) `C6_BLOCKED_NO_STABLE_CODIM2_SCREEN`, routing to (2). Given failure mode #1, the more defensible reading is that **"stability" of the screen family — not just bare existence for isolated pairs — is exactly what's unproven**, which is what terminal (1) tests. Choosing (2) implicitly grants the object more standing than the evidence supports — precisely what would make it an attractive re-entry point for a future `CANDIDATE_6`. Precedence (§17) is applied mechanically/consistently, but the Gate-1 factual predicate feeding it is contestable.
- **Premature / over-broad claims:** (i) §18 unqualified non-existence — over-broad. (ii) C5 is **not** reopened under a new name in any structural sense (§12 table accurate) — that accusation does not stick. (iii) No codim-2/expansion/trapping/reconstruction smuggled as *established* — §6/§10/§19 disclaim consistently. (iv) The real over-claim is narrower: Gate-1 abundance inflation (#1) compounding with the C4-inheritance analogy (#2) and the universal-language slip (#3) — three over-reaches that together make the negative sound more thoroughly earned than the internal evidence shows.
- **Independent-falsification gate:** Weak. Three of four Wave-1 experts "Endorse" without flagging the Gate-1 abundance self-contradiction (#1) or the C4-inheritance over-reach (#2) — both catchable from the text, no execution needed. Only the Logician flagged the interpretive-judgement and universal-language issues; none closed the abundance loop. Recommend the chair require an explicit **correction pass on §7 and §18** before treating 043 as settled, not just a "noted for future work" caveat.
- **Minimal falsification test (thought-experiment, no execution/seeds):** Take a single totally ordered chain `c₁≺c₂≺…≺cₙ` (n≥5). Its `≺*` = consecutive pairs. For any internal `(p,q)=(cᵢ,cⱼ)` with `j-i≥2`: unique link-child of `p` is `cᵢ₊₁`; unique link-parent of `q` is `cⱼ₋₁`. `W = {cᵢ₊₁} ∩ {cⱼ₋₁}` = **singleton** iff `j-i=2`, **empty** for `j-i≥3`. Of `O(n²)` internal pairs, only `O(n)` yield `W≠∅`, each a singleton (never a larger antichain). This **falsifies** the "Plausiblemente ALTA / una fracción [de O(N²)]" claim in §7 even in a maximally comparable causet, and demonstrates the honest Gate-1 finding is "exists only on a sparse, singleton-dominated sub-slice of `P_int`" — the evidentiary basis for routing to `C6_BLOCKED_NO_STABLE_CODIM2_SCREEN` rather than conceding a stable/abundant family. (A branchier hand poset — two parallel 3-chains merged at top and bottom — shows `|W|` can exceed 1, but does not rescue the `O(N²)`-fraction abundance claim, since link out-degree stays combinatorially bounded per node.)

## 6. Pre-registration verdict
### Pre-registration verdict
- **Verdict: PASS**
- **Freeze status:** 043 freezes nothing and introduces no threshold. Header (`:3-4`): `STATUS: CONCEPT_ADJUDICATION_DOCUMENTARY / CANDIDATE_6_NOT_OPENED`, `NO_IMPLEMENTATION / NO_SYNTHETIC_EXECUTION / NO_SEEDS / NO_FREEZE / NO_CONTRACT`. §19 (`:499-501`): "No se congeló ni tocó ningún contrato. No se modificó `thresholds.py`, manifests, ledgers, terminales ni evidencia." Gate 3 Mechanism B explicitly *rejects* a free threshold ("un umbral libre … prohibido por calibración", `:266-268`) rather than introducing one.
- **Seal integrity:** Independently recomputed `sha256sum thresholds.py` = `6e2c3888…`, matching the frozen seal in `docs/preregistration_002.md:7-8` and `docs/preregistration_002_result.md:21,80` (prereg-002 seal #3, commit `573cfcb`). `git diff --stat HEAD` on `thresholds.py` and all seal/prereg docs: zero changes. 043 runs no sealed path (§19, `:499-501`).
- **Seed discipline:** No seeds consumed, no reserved band touched (§19, `:499`). Dev/validation separation untouched; pure concept doc reasoning over existing frozen sources without invoking validation-band data.
- **Reporting rule:** Terminal `C6_BLOCKED_NO_INTRINSIC_SCREEN_TRANSPORT` (§18, `:481`) reported plainly as a blockage. The `W(p,q)=∅` case handled per abstention rule, not conflated with a physical result: §4.7 (`:115-117`) "Es una abstención del objeto … nunca 'área cero' física", echoed in §11.1 (`:342-343`).
- **Forbidden moves present? None found.**
  - C5 reopening on twin minimal partitions: disclaimed — §12 table marks Twins/Bridges/Bloques-de-minimales/Matrices-de-futuros-comunes all `NO` (`:369-378`); §3 states `Min(C)→región` (exhausted) vs `(p,q)→W(p,q)` (new object).
  - C4 reopening by calling all-pairs "neighbors": not done — §9.1 identifies the pairing as the unresolved lateral-neighbor structure and declares it **Falla** (`:256-261`).
  - "Fixing" F3 with ε-clustering / unresolved neighbor graph: not done — §9.2 rejects any overlap threshold as "prohibido por calibración" (`:266-268`); §12 (`:384-385`) explicitly respects the C42 §6 prohibition.
  - CANDIDATE_6 opening: every `CANDIDATE_6` occurrence is negated (`:3, :20, :482, :493, :516`); `grep` confirms no bare/affirmed opening.
  - Forbidden terminal tokens (FROZEN/APPROVED/IMPLEMENTATION_AUTHORIZED/SEEDS_AUTHORIZED/TRAPPING_DETECTED/HORIZON_LOCALIZED/empirical PASS): none in §18/§19/final status (`:509-518`); §6 disclaims codim-2/expansion/marginality/trapping (`:166-169`); §10 disclaims signed ΔA/trapping (`:332-333`).
  - Reconstruction over-claim: none (§6, §19, `:503-505`); consistent with `NO_RECONSTRUCTION_CLAIM`.
- **Reasons:**
  - Decision 042 §6 (`:188-192`) is the operative prohibition list; 043 §12 table + `:384-385` track and satisfy all three items.
  - Decision 042 §7 menu is "Ninguna autorizada"; 043 executes no menu item as authorized, staying at conceptual adjudication.
  - `C6_BLOCKED_NO_INTRINSIC_SCREEN_TRANSPORT` is a novel-but-clearly-BLOCKED terminal in the same family as prior C-line closures; not on the forbidden list.
  - Seal SHA recomputed matches the frozen seal; zero drift on any seal/threshold/prereg file; 043 is the sole untracked artifact.

## 7. Literature verdict
### Literature verdict
| Citation | Claimed by | Status |
|---|---|---|
| EGS link def `x≺*y ⟹ ¬∃z:x≺z≺y` (md:100) | mathematician / repo antichain validity | CONFIRMED |
| EGS longest-chain-from-minimals splits interior/exterior (md:181,188,191) | EGS event-horizon diagnostic | CONFIRMED |
| EGS "no spatial two-surfaces in 1+1D, use 1D distance" (md:227-230) | codim-2 unsupported in 1+1D | CONFIRMED |
| EGS Θ=(1/A)dA/dλ eq.11 (md:217) | continuum expansion definition | CONFIRMED |
| EGS Θ_out=(1/r)(1−2M/r), sign change at r=2M, eq.12 (md:223-225) | apparent-horizon sign criterion | CONFIRMED |
| EGS Def. 1 causal ladder, 2nd rung, p1≺q2 "not linked" (md:238-249) | ladder construction | CONFIRMED |
| EGS discrete expansion = ladder-ensemble transverse-distance obs. (md:276-289) | EGS expansion ≠ W(p,q) | CONFIRMED (Eq.14 `E_I^{ab,n}` is a sum over an ensemble of ladders J vs a fixed reference ladder I) |
| EGS: only the SIGN of Θ recovered after averaging (md:344-347) | limits of the observable | CONFIRMED |
| EGS constant sprinkling density from constant-determinant metric (md:130,135) | sprinkling construction | CONFIRMED |
| Repo link relation `L = C & ~((C·C)>0)` at `dev/measure_kbeam_peeloff.py:111` (CSR ~121/130/131-137) | transitive-reduction/link def used by C6 | CONFIRMED (line ~121/130 vs cited :111; 043 also cites :111; content correct, minor drift) |
| Lean `IsCover x y := x<y ∧ ¬∃z, x<z<y` (`formal/HorizonFormal/HorizonFormal/Horizon.lean:56`) | antichain/link validity in Lean | CONFIRMED (exact at 56-57) |
| No `W`/`Waist`/`Alexandrov`/antichain-lemma object in project Lean | novelty of 043's object | CONFIRMED (`grep` over project `.lean` → zero hits; no `def W`) |
| Decision 039: C4 rejection scoped to two audited families, "not a theorem" (039:400-403) | scope-limit of inherited C4 impossibility | CONFIRMED (lines 384-401) |
| Benincasa–Dowker interval-abundance `C_k` as curvature/manifold invariant | curvature contrast for §14 | UNCONFIRMED as labeled — sources use `N_k` for abundances; `C_k^{(d)}` are the action *coefficients*. Substance correct; **043 §14 avoids this error** (uses generic "abundancias de intervalos", no `C_k` symbol) — defect is only at dossier-shorthand level |
| PR009 closure = FAILED_DATA_CONTRACT before scoring (closure decision:3-4,13-20,52-61) | PR009 not a physical negative | CONFIRMED |
- Notes: Only one substantive issue — the "BD interval-abundance `C_k`" shorthand mislabels the symbol (`N_k` are the abundances; `C_k^{(d)}` are action coefficients). This is a dossier/paraphrase naming error, **not** in decision 043 (§14 uses "abundancias de intervalos"). All other cited claims (9 EGS sub-citations, the repo link code, Lean `IsCover` + absence of a `W` object, decision 039's scope limitation, the PR009 closure) were opened and CONFIRMED, modulo immaterial few-line code-anchor drift. Read-only; no seal/freeze artifact touched.

## 8. Synthesis
**Where the panel agrees (unanimous or near-unanimous):**
- **The antichain claim is a genuine, valid order-theoretic theorem** under the repo's frozen link relation (`L = C & ~((C·C)>0)`, `dev/measure_kbeam_peeloff.py:111`). Mathematician, logician and physicist independently audited the §5 proof and found no gap; literature verifier confirmed the underlying `IsCover` definition and that no equivalent object is machine-checked. Caveat: it is a *prose* theorem, not Lean-verified.
- **The object is well-posed, decidable, and relabel-invariant.** `W(p,q)` is cleanly a SET (the set of length-2 link-chain midpoints), never conflated with a physical surface. `|W|` is a closed, relabel-invariant *provisional* cardinality — explicitly not physical area, not demonstrated codim-2. Gate-2's bounded pass is correct.
- **Discipline is clean.** Warden **PASS**, seal independently recomputed and matched to prereg-002 seal #3; reproducibility confirms a single untracked artefact, no committing footprint, `NO_IMPLEMENTATION/NO_SEEDS` honoured. Every decision-042 §6 prohibition is tracked and satisfied; all `CANDIDATE_6` occurrences negated; no forbidden terminal token; `W=∅` reported as abstention.
- **Physics boundaries are correctly drawn against EGS §IV** and all EGS citations CONFIRMED: codim-2 is physically unsupported in a 1+1D patch (no spatial 2-surface); EGS expansion is measured along a *traced ladder with identity*, a different object from a single interval's waist, so EGS focusing does not by itself supply transport; the PR009 beam is not reused and its `FAILED_DATA_CONTRACT` closure is not a physical negative.
- **C5 is not reopened under a new name.** The §12 non-dependence table is accurate (no common-future matrices of minimals, twins, bridges, minimal blocks, or region map). The falsifier's own review confirms this accusation "does not stick."

**Open disagreements (surfaced, not hidden):**

1. **The strength of the negative is over-stated in three specific places** (falsifier + logician; mathematician's endorsement is explicitly "*as scoped*"):
   - **(a) Gate-1 abundance — an internal inconsistency.** §7 calls the screen family "Plausiblemente ALTA", but §4.6 and §15.1-2 concede abundance is unresolved without execution. The falsifier's hand-poset (a total chain) proves `W` is empty or singleton on all but an `O(N)` slice, never larger than a point — a singleton is not a codim-2 surface even by the doc's own §11.2. This is a contradiction inside the document, not a caveat.
   - **(b) The C4/C5 "inherited impossibility" is heritage-analogy, not theorem transfer.** Decision 039 is explicitly scoped to two audited families and "not a theorem", and explicitly declines to refute "2-link methods" — which is arguably what `W`'s A-mechanism pairing is. The reduction "closed transport ⟹ closed lateral neighbor graph" is proved only for mechanism A's symmetric variant; asserted in general.
   - **(c) §9.5/§18 report a finite-menu (A–D) refutation with universal ∀-language** ("no existe un transporte order-only cerrado"), when the honest scope is "no closed *canonical/single-valued* transport among the examined mechanisms + an asserted common obstruction".

2. **Terminal-selection fork (the one substantive science question).** The doc routes past terminal (1) `C6_BLOCKED_NO_STABLE_CODIM2_SCREEN` by asserting an abundant family, then lands on (2) `C6_BLOCKED_NO_INTRINSIC_SCREEN_TRANSPORT`. If abundance/stability is unresolved (disagreement 1a), the precedence rule 1 predicate ("no admite interpretación mínima como pantalla *estable/codim-2*") is arguably satisfied, and precedence says the **first** applicable terminal wins → (1). The mathematician and physicist accept (2) *as scoped* (a non-trivial family of antichains does exist, and transport failure is an independent, robust block that holds even for the `|W|≥2` screens that exist); the falsifier and logician argue (1) is the more defensible doc-only negative because a *stable codim-2* screen cannot be established without execution. **Both terminals are BLOCKED negatives; C6 and CANDIDATE_6 stay closed either way — the scientific outcome is invariant. The dispute is which honest label enters the ledger.**

**Chair's reading.** The core of 043 is sound and the discipline is impeccable, but a red-team pass found a genuine internal inconsistency (1a) that feeds directly into the terminal choice (fork 2). Per the founding rule "a guardrail that cannot fail is decoration" and "fracaso documentado > almost-PASS" (decision 042 §5.7), an adjudication whose *function* is to foreclose future work must not over-state the strength of its own negative. The fixes are bounded, doc-only edits — but because they change which terminal is honest, this should be an explicit revision, not a silently-appended caveat. Verdict: **`RECOMMEND_REVISE_AND_RECONVENE`**. The terminal polarity (C6 blocked, CANDIDATE_6 not opened) is not in doubt; the revision settles the label and removes the over-claims.

**Chair's recommendation on the fork:** adopt the more conservative terminal **(1) `C6_BLOCKED_NO_STABLE_CODIM2_SCREEN`**, and record the transport failure (§9–§10) as an *additional, independent* block rather than the primary one — this grants the object no more standing than doc-only evidence supports and keeps the transport finding on the ledger. Terminal (2) remains acceptable *only if* the revision adds an explicit order-theoretic argument that a non-trivial `|W|≥2` screen family exists (existence, not abundance) sufficient to clear precedence rule 1 — and even then §7/§18 must be scoped. This is genuinely the user's call (both are honest negatives), so it is put to the user in §9.

## 9. Next-step spec
**This is a doc-only adjudication review. There is no committing step, no execution, no seeds — nothing here touches the sealed path.** All steps below are **reversible** edits to `docs/comite/comite_decision_043_*.md` (and this brief), to be performed **only if the user authorises**, after the user resolves the terminal fork.

**Reversible correction pass on decision 043 (on user authorisation):**
1. **Fix the Gate-1 inconsistency (§7).** Replace "Abundancia … Plausiblemente ALTA" with an honest statement: *existence* of the antichain family is proven (§5), but *abundance and `|W|≥2` stability are UNRESOLVED without execution*; cite the total-chain counterexample (singleton/empty on all but an `O(N)` slice) as the reason. Remove the "fracción de O(N²)" claim.
2. **Scope the no-transport language (§9.5, §18, one-line summary).** State "no closed *canonical / single-valued / identity-preserving* order-only transport among the examined mechanisms A–D, plus an asserted common obstruction" — not universal non-existence. Explicitly note (per the mathematician) that a *branching* order-only relation trivially exists; what fails is a canonical one.
3. **Re-label the C4/C5 inheritance (§12) as heritage-analogy.** Note decision 039 is scoped to two audited families, is "not a theorem", and declines to refute "2-link methods"; therefore the transport→lateral-pairing reduction is *proved for mechanism A's symmetric variant, conjectured in general*, and the C4/C5 heritage is a plausibility argument, not a theorem transfer.
4. **Resolve the terminal per the user's fork answer** (see AskUserQuestion): either change the terminal to `C6_BLOCKED_NO_STABLE_CODIM2_SCREEN` with transport recorded as an independent additional block (chair's recommendation), or keep `C6_BLOCKED_NO_INTRINSIC_SCREEN_TRANSPORT` after adding the explicit existence-of-`|W|≥2`-family argument that clears precedence rule 1.
5. **Hygiene (reproducibility nit):** embed `HEAD=050a4e2` and the seal SHA `6e2c3888…` inline in 043 for anchor-drift resistance.
6. **Dossier-only fix (does not touch 043):** if the BD interval-abundance citation is ever reused verbatim, correct the symbol `C_k → N_k`.

**No committing steps.** No implementation, no synthetic contract, no seeds, no freeze, no `CANDIDATE_6` opening is proposed or authorised. Any future C6 opening remains a separate, higher-bar decision.

**Minimal falsification test already discharged (no execution needed):** the falsifier's total-chain hand-poset (`c₁≺…≺cₙ`, `W(cᵢ,cⱼ)` = singleton iff `j-i=2`, else empty) is a pen-and-paper proof that the family is singleton/empty-dominated in the low-branching regime. It is the concrete evidence behind the Gate-1 correction and the terminal fork; it requires no seeds and can be included verbatim in the revised §7/§11.

## 10. Verdict
COMMITTEE_DECISION_VERDICT=RECOMMEND_REVISE_AND_RECONVENE

## 11. User sign-off
- **2026-07-21 — user (adnacho@gmail.com) resolved the two forks put in §9:**
  - **Terminal:** switch decision 043 to **`C6_BLOCKED_NO_STABLE_CODIM2_SCREEN`** (chair's recommended, more conservative negative); the transport failure is recorded as an *independent additional block*, not the primary terminal.
  - **Corrections:** apply **all** over-claim corrections + hygiene to decision 043 (§7 abundance `ALTA → UNRESOLVED` with the chain counterexample; §9.5/§18 scoped to "no closed canonical transport among examined mechanisms"; §12 C4/C5 inheritance re-labelled heritage-analogy per decision 039's scoping; HEAD+seal SHA embedded).
- **Applied:** the revision was executed on `docs/comite/comite_decision_043_...md` in this session (reversible doc-only edits; no execution, no seeds, no freeze, seal untouched). `CANDIDATE_6` remains not opened; C1–C5 line remains closed and not reopened.
- _(Space left for any further overriding notes.)_
